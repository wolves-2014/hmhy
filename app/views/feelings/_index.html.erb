<% if @feelings %>
  <div class="word-container" id="<%= @feelings.first.ranking %>">
    <% @feelings.each do |feeling| %>
      <button type="button" class="btn btn-info feeling-word"><%= feeling.word %></button>
    <% end %>
  </div>
<% end %>
